{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run db:push && npm run import && npm run build",
  "build": {
    "env": {
      "DATABASE_URL": "file:./prisma/dev.db"
    }
  },
  "env": {
    "DATABASE_URL": "file:./prisma/dev.db"
  },
  "headers": [
    {
      "source": "/(.*)",
      "headers": [{ "key": "X-Robots-Tag", "value": "noindex" }]
    }
  ]
}
